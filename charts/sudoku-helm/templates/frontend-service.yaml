apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.containers.frontend.appName }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.containers.frontend.appName }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.containers.frontend.appName }}
  ports:
    - port: {{ .Values.containers.frontend.ports.external }}
      targetPort: {{ .Values.containers.frontend.ports.internal }}
      name: {{ .Values.containers.frontend.appName }}